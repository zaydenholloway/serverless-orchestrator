function executeTask(taskName) {
  console.log(`Running task: ${taskName}`);
  return {
    task: taskName,
    status: "completed"
  };
}

const result = executeTask("Data Processing");
console.log(result);
